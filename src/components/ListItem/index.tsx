// import { useState } from 'react';

// interface ListItemProps {
//   isChecked: boolean;
//   setIsChecked: (isChecked: boolean) => void;
//   className?: string;
// }

// const ListItem = ({ isChecked, setIsChecked, className }: ListItemProps) => {
//   const pStyle = 'px-2 py-4 text-gray-500 font-semibold flex gap-2 justify-center cursor-pointer';
//   const warn = 'text-red-500';
//   const primary = 'text-blue-500';
//   const secondary = 'text-gray-500';

//   const handleClick = () => {
//     setIsChecked(!isChecked);
//   };

//   return (
//     <li className={`${pStyle} ${className}`} onClick={handleClick}>
    
//     </li>
//   );
// };

// export default ListItem;
